{
  "name": "dual-emitter",
  "version": "0.4.1",
  "description": "EventEmitter done right and no dependencies. For nodejs and the browser (>= IE8). Can emit custom or DOM events.",
  "repository": "tunnckoCore/dual-emitter",
  "author": "Charlike Mike Reagent <@tunnckoCore> (http://www.tunnckocore.tk)",
  "main": "index.js",
  "license": "MIT", 
  "scripts": {
    "test": "standard && node test.js",
    "build": "npm run browserify && npm run uglify",
    "browserify": "browserify -s DualEmitter -do dist/dual-emitter.js index.js",
    "uglify": "uglifyjs -m -c -o dist/dual-emitter.min.js dist/dual-emitter.js"
  },
  "dependencies": {},
  "devDependencies": {
    "assertit": "^0.1.0"
  },
  "keywords": []
}
